services:
  aethel:
    build: .
    image: aethel:latest
    container_name: aethel-app
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
